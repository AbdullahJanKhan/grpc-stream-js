version: "3"
services:
  reverse-proxy:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs/server-cert.crt:/etc/ssl/certs/
      - ./certs/server-private.key:/etc/ssl/private/
    ports:
      - 8000:80
    restart: always
